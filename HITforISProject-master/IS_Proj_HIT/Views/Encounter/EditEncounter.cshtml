@model IS_Proj_HIT.Models.Encounter

<partial name="PatientBannerPartial" model="ViewBag.Patient" />
<div class="card">
    <div class="card-header bg-primary text-white">
        <h4>
            Edit Encounter
            <a asp-action="ViewEncounter"
               asp-route-encounterId="@Model.EncounterId"
               class="btn btn-info btn-sm float-right">
                Return to Encounter View
            </a>
        </h4>
    </div>
    <div class="card-body bg-light">
        <form asp-action="EditEncounter" id="editEncounter" method="post" name="editEncounter">
            <div asp-validation-summary="All" class="container"></div>
            <input asp-for="@Model.EncounterId" name="EncounterId" type="hidden" value="@Model.EncounterId" />
            <input asp-for="AdmitDateTime" name="AdmitDateTime" type="hidden" value="@Model.AdmitDateTime" />
            
            <partial name="EncounterFormFields" model="@Model" />
            <hr/>
            <div class="row">
                <div class="col-sm-12 col-lg-2">
                    <label>Discharge Time</label>
                    <input type="datetime" asp-for="@Model.DischargeDateTime" name="DischargeDateTime" class="form-control" placeholder="Discharge Time">
                    <span asp-validation-for="DischargeDateTime" class="text-danger"></span>
                </div>
                <div class="col-sm-12 col-lg-10">
                    <label>Discharge Disposition</label>
                    <select class="form-control dropdowns" asp-for="@Model.DischargeDisposition" asp-items="ViewBag.Discharges" name="DischargeDisposition">
                        <option value="">Select Discharge Disposition</option>
                    </select>
                    <span asp-validation-for="DischargeDisposition" class="text-danger"></span>
                </div>
            </div>
            <hr />

            <button class="btn-primary" type="submit">Save</button>
        </form>
    </div>
</div>
